Algoritmo "Questao 6"

Var

dmaior,dmenor,area:real

Inicio
escreval("Informe o valor da diagonal maior (m): ")
leia(dmaior)

escreval("Informe o valor da diagonal menor (m): ")
leia(dmenor)

area <- (dmaior*dmenor)/2

escreval("A area do losango e:",area," metros quadrados")

Fimalgoritmo